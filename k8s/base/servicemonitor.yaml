apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: metro-timetable
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: metro-timetable
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
