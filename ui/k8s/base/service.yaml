apiVersion: v1
kind: Service
metadata:
  name: metro-timetable-ui
  labels:
    app: metro-timetable-ui
spec:
  selector:
    app: metro-timetable-ui
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
